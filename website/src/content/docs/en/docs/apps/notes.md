---
title: Notes
description: Documents written in Markdown, one file per note.
sidebar:
  order: 3
---

A note is a document written in Markdown, and each note is one file. The list on the left shows the most recently updated notes first, and search looks through the text of the notes too. On the right, the Markdown is rendered (tables and checkboxes show as they are). Click "Edit" to change the Markdown itself, and press ⌘S to save. Deleting goes through a confirmation screen and moves the file to the macOS Trash, so you can restore it from Finder while it is still in the Trash.

In the conversation, "Take a note" writes a new note, and "Do I have a note about …?" finds one and reads it. You can't rewrite or delete an existing note from the conversation. Notes are not added to every conversation, and they are separate from memory.
