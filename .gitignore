node_modules/
out/
# The compiled helper that scripts/build-native-mic.sh generates.
resources/native/asist-mic
dist/
coverage/
# The pictures npm run demo:fit takes of what did not fit, on CI.
fit-shots/
.env
.env.*
!.env.example
*.tsbuildinfo
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

resources/native/asist-calendar
# The uv and git that scripts/fetch-uv.sh and scripts/build-git.sh put in the app.
resources/uv/
resources/git/
# The permission texts that scripts/macos/permission-texts.mjs generates.
build/lproj/
# The licenses of the bundled packages that scripts/third-party-notices.mjs writes after each build.
build/THIRD_PARTY_NOTICES.txt

# Python bytecode.
__pycache__/

# The login and the local state of wrangler (scripts/wrangler.mjs keeps its login in .wrangler/config).
.wrangler/

# What imagegen returns for the launch video, and the pictures passed to it. The committed props and
# characters are cut from the first by tools/cutout.py; tools/refs.py makes the second from the website.
promotions/launch-video/assets/gen/raw/
promotions/launch-video/assets/gen/refs/
